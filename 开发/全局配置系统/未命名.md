# 设计目标
多配置文件支持，配置文件别名
Schema 驱动的注册制
强类型访问
变更订阅与通知
即时生效 vs 重启生效

# notes

ConfigTraits 可以利用json的宏实现配置结构体的自动序列化
ConfigTraits 应该收集配置类所在文件，以及属性路径
配置属性统一路径：文件别名：属性路径，路径的解析存储参考StringName


# Architecture

PropertyTree（YAML::Node）
ConfigSourceStore
ConfigManager

ConfigSchema
