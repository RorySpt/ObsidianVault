```
【主要类和接口】

   1. PropertyBase - 属性基类（第244行）
      - 虚函数：get_container_type()、clone()、collect_references()等
      - 容器操作：get_container_size()、add_element()、clear()等
      - Vector操作：modify_element()、get_element_at()、remove_element_at()等
      - Map操作：get_map_keys()、get_map_value()、modify_map_key()等
      - 枚举操作：get_enum_item_names()、get_enum_name()、set_enum_by_name()等
      - 属性信息：get_name()、get_type()、get_display_name()、get_category()等
      - 数值限制：get_clamp_min()、get_clamp_max()、get_delta()等

   2. Property<T> - 基础属性模板类（第436行）
      - 类型定义：GetterType、ConstGetterType、SetterType
      - 核心方法：get()、set()、clone()、collect_references()等
      - 序列化：serialize_value()、on_serialize()
      - 引用收集：collect_property_references()、collect_element_references()

   3. EnumProperty<Enum> - 枚举属性类（第603行）
      - 类型定义：GetterType、ConstGetterType、SetterType
      - 枚举操作：get_enum_item_names()、get_enum_name()、set_enum_by_name()
      - 核心方法：get()、set()、clone()、collect_references()等

   4. ContainerPropertyBase<ContainerType> - 容器属性基类（第719行）
      - 空基类，用于继承

   5. VectorProperty<T> - Vector容器属性（第725行）
      - 类型定义：GetterType、SetterType、GetterPointerType、CallBackMethod
      - 容器操作：get_container_size()、get_element_at()、add_element()、clear()等
      - 元素操作：modify_element()、remove_element_at()
      - 核心方法：get()、set()、clone()、clone_element()等
      - 引用收集：collect_references()、collect_element_references()

   6. MapProperty<Key, Value> - Map容器属性（第917行）
      - 类型定义：GetterType、SetterType、GetterPointerType、CallBackMethod
      - Key操作：copy_key_ptr()、delete_key_ptr()、modify_map_key()
      - Value操作：get_map_value()、modify_map_value()、remove_map_value()
      - 容器操作：get_container_size()、get_map_keys()、add_element()、clear()等
      - 核心方法：get()、set()、clone()、clone_element()等

   7. SetProperty<T> - Set容器属性（第1185行）
      - 类型定义：GetterType、SetterType、GetterPointerType、CallBackMethod
      - 容器操作：get_container_size()、add_element()、clear()等
      - 核心方法：get()、set()、clone()、clone_element()等

   8. MetaInfo - 元信息类（第1383行）
      - 属性查询：has_property()、has_property_recursive()
      - 函数查询：has_function()、has_function_recursive()
      - 属性访问：get_property<T>()、set_property<T>()、get_pointer_property()
      - 函数调用：invoke<...>()
      - 继承关系：is_derived_from()、is_base_of()
      - 克隆：clone()
      - 引用收集：collect_references()、collect_weak_references()
      - 成员数据：bases、type_name、properties、functions、member_size

   9. Object - 对象基类（object.h 第28行）
      - 反射元信息：set_meta_info()、get_meta_info()、get_type_name()
      - 类型检查：is_type<T>()、is_derived_from<T>()、is_base_of<T>()、is_a<T>()
      - 序列化：serialize()、on_serialize()、on_load()、on_save()
      - 宏：GENERATED_BODY_PLAIN(Object)

   10. Struct - 结构体基类（object.h 第85行）
       - 继承自Object
       - 标记：using IsStruct = void
       - 宏：GENERATED_BODY_PLAIN(Struct)

   11. Class - 类基类（object.h 第96行）
       - 继承自Object
       - 标记：using IsClass = void
       - 对象管理：get_id()、set_name()、get_name()
       - 激活状态：set_active()、is_active()、is_destroyed()
       - 生命周期：add_to_root()、remove_from_root()、clone()、destroy()
       - 引用收集：collect_references()、collect_weak_references()
       - 虚函数：on_init()、on_destroy()、on_activate()、on_deactivate()
       - 宏：GENERATED_BODY_PLAIN(Class)

   【关键宏定义】

   GENERATED_BODY_PLAIN(CLASS_NAME) - 为Object/Struct/Class自动添加反射相关逻辑
   - 提供：s_meta_info、get_static_meta_info()、get_static_type_name()
   - 属性访问：get_property<T>()、set_property<T>()、get_pointer_property()
   - 函数调用：invoke()

   GENERATED_BODY(CLASS_NAME) - 继承GENERATED_BODY_PLAIN并添加构造函数辅助

   CLASS(...) - 标记反射类
   STRUCT(...) - 标记反射结构体
   ENUM(...) - 标记反射枚举
   PROPERTY(...) - 标记反射属性
   FUNCTION(...) - 标记反射函数

   【属性标志枚举】

   enum EPropertyFlags : uint8_t
   {
       None = 0,
       EditAnywhere = 1 << 1,      // 可在编辑器编辑
       VisibleAnywhere = 1 << 2,   // 可见但不可编辑
       BlueprintReadOnly = 1 << 3, // 蓝图只读
       BlueprintReadWrite = 1 << 4 // 蓝图读写
   };

   【容器类型特征】

   IsTVector<T> - 检测TVector<T>类型
   IsTMap<K,V> - 检测TMap<K,V>类型
   IsTSet<T> - 检测TSet<T>类型

   【对象类型特征】

   IsObjectType<T> - 检测Object类型
   IsStructType<T> - 检测Struct类型
   IsClassType<T> - 检测Class类型
   IsObjectPointer<T> - 检测Object指针类型
   IsPointer<T> - 检测指针类型
   IsObjectVector<T> - 检测Object向量类型

   【全局辅助函数】

   void collect_member_references(const void* instance, std::unordered_set<void*>& references);
   void* clone_object(void* original_object, bool filter_assets = true);
   void clone_struct(void* original_struct, void* target_struct);
   template <typename T> T clone_value(T& element, PropertyBase* element_property = nullptr);
   template <typename ObjectType> bool is_derived_from(MetaInfo* meta_info);
   template <typename ObjectType> bool is_base_of(MetaInfo* meta_info);

   【全局数据结构】

   extern std::unordered_map<std::string, EnumHelper*> g_enum_helpers;
   extern std::unordered_map<std::string, std::function<Class*()>> g_class_factories;
   extern std::unordered_map<std::string, std::function<Object*(Object*, const std::string&)>> g_property_getters;
   extern std::unordered_map<void*, void*> g_cloned_ids;

   【EnumHelper 类】

   class ZETA_API EnumHelper
   {
   public:
       EnumHelper(std::string type_name, std::vector<EnumValue> items);
       const std::string& get_type_name() const;
       const std::vector<std::string>& get_item_names() const;
       const std::string& get_name_by_value(int64_t enum_value);
       int64_t get_value_by_name(const std::string& enum_item_name);
   };

   【ArgumentWrapper 类】

   class ArgumentWrapper
   {
   public:
       virtual ~ArgumentWrapper() = default;
       virtual const void* get() const = 0;
       virtual void* get() = 0;
   };

   template <typename T>
   class TArgumentWrapper : public ArgumentWrapper
   {
       // 用于函数调用参数包装
   };

   【MetaInfo::FunctionInfo 结构体】

   struct FunctionInfo
   {
       std::string category;
       bool call_in_editor;
       FunctionType function_type;
   };

   【关键接口方法总结】

   PropertyBase 的 get/set 方法：
   - T& get(void* instance) - 获取属性值
   - const T& get(const void* instance) const - 获取常量属性值
   - void set(void* instance, const T& value) const - 设置属性值
   - void* get_pointer_property(void* instance, const std::string& name) - 获取指针属性

   MetaInfo 的 get/set 方法：
   - T& get_property<T>(void* instance, const std::string& name) const
   - const T& get_property<T>(const void* instance, const std::string& name) const
   - void set_property<T>(void* instance, const std::string& name, const T& value) const
   - void* get_pointer_property(void* instance, const std::string& name)

   Object 的 get/set 方法（通过 GENERATED_BODY_PLAIN 宏）：
   - T& get_property<T>(const std::string& name)
   - const T& get_property<T>(const std::string& name) const
   - void set_property<T>(const std::string& name, const T& value)
   - void* get_pointer_property(const std::string& name)

   【容器操作接口】

   Vector 操作：
   - size_t get_container_size(void* vector_pointer) const
   - void* get_element_at(void* vector_pointer, size_t index) const
   - bool modify_element(void* vector_pointer, size_t index, void* value, std::string& out_message) const
   - bool remove_element_at(void* vector_pointer, size_t index, std::string& out_message) const
   - void add_element(void* vector_pointer) const
   - void clear(void* vector_pointer) const

   Map 操作：
   - std::vector<const void*> get_map_keys(void* map_pointer) const
   - void* get_map_value(void* map_pointer, const void* key, std::string& out_message) const
   - bool modify_map_key(void* map_pointer, const void* old_key, const void* new_key, std::string& out_message) const
   - bool modify_map_value(void* map_pointer, const void* key, void* value, std::string& out_message) const
   - bool remove_map_value(void* map_pointer, const void* key, std::string& out_message) const
   - void* copy_key_ptr(const void* key) const
   - void delete_key_ptr(void* key) const

   Set 操作：
   - size_t get_container_size(void* set_pointer) const
   - void add_element(void* set_pointer) const
   - void clear(void* set_pointer) const
```